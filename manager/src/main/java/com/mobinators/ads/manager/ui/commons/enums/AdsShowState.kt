package com.mobinators.ads.manager.ui.commons.enums

enum class AdsShowState {
    APP_PURCHASED,
    NETWORK_OFF,
    ADS_OFF,
    ADS_STRATEGY_WRONG,
    ADS_ID_NULL,
    TEST_ADS_ID,
    ADS_LOAD_FAILED,
    ADS_DISMISS,
    ADS_DISPLAY_FAILED,
    ADS_DISPLAY,
    ADS_IMPRESS,
    ADS_CLICKED,
    ADS_CLOSED,
    ADS_OPEN
}

enum class AdsLoadingState {
    APP_PURCHASED,
    NETWORK_OFF,
    ADS_OFF,
    ADS_STRATEGY_WRONG,
    ADS_ID_NULL,
    TEST_ADS_ID,
    ADS_LOADED,
    ADS_LOAD_FAILED
}